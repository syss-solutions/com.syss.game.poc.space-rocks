/// @description Rock Creation

// Speed to 1, very low compared to the bullet’s speed.
speed = 1;

// This can be any random number from 0 to 360.
direction = random(360);

// Rotate the sprite a random number too.
image_angle = random(360);